# =================================================================
# Application Configuration
# =================================================================
# アプリケーションの基本設定

# アプリケーションのベースURL（Open Graph、メタデータなどで使用）
# 本番環境: https://v-chat.uomi.dev
# 開発環境: http://localhost:3000
NEXT_PUBLIC_BASE_URL=https://v-chat.uomi.dev

# =================================================================
# Firebase Configuration
# =================================================================
# クライアントサイド用のFirebase設定
# 取得方法: Firebase Console → プロジェクト設定 → 全般 → ウェブアプリ
#
# 注意: これらの値は公開されても問題ありません（Firebaseセキュリティルールで保護）
# ただし、本番環境では必ず実際の値に置き換えてください

NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=

# =================================================================
# Firebase Admin SDK（サーバーサイド専用）
# =================================================================
# API Routes、Cloud Functionsなどサーバーサイドでのみ使用
#
# 取得手順:
# 1. Firebase Console (https://console.firebase.google.com/) を開く
# 2. プロジェクトを選択
# 3. 左上の歯車アイコン → 「プロジェクトの設定」をクリック
# 4. 「サービスアカウント」タブをクリック
# 5. 「新しい秘密鍵の生成」ボタンをクリック
# 6. ダウンロードされたJSONファイルから以下の値をコピー:
#    - "project_id" → FIREBASE_PROJECT_ID
#    - "client_email" → FIREBASE_CLIENT_EMAIL
#    - "private_key" → FIREBASE_PRIVATE_KEY
#
# 注意:
# - private_keyの改行は '\n' のまま（置換不要）
# - 例: "-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n"
#
# ⚠️ 警告: これらの値は絶対に公開しないでください！
# 　　　　 GitやVercelなどの環境変数として安全に保管してください

FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Emulators are for local development only. Uncomment to enable:
# NEXT_PUBLIC_FIREBASE_EMULATOR=true # set to "true" for local dev only
# FIRESTORE_EMULATOR_HOST=localhost:8080
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099

# =================================================================
# VRoid Hub OAuth Configuration
# =================================================================
# VRoid Hubからアバターモデルを取得するための設定
# 取得方法: https://developer.vroid.com/ でOAuthアプリケーションを作成
#
# リダイレクトURI: http://localhost:3000/api/auth/callback/vroid

VROID_CLIENT_ID=
VROID_CLIENT_SECRET=
NEXT_PUBLIC_VROID_CLIENT_ID=

# =================================================================
# NextAuth Configuration
# =================================================================
# 認証システムの設定
#
# NEXTAUTH_SECRET: ランダムな文字列を生成（例: openssl rand -base64 32）
# NEXTAUTH_URL: 本番環境では実際のURLに変更

NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=
NEXTAUTH_DEBUG=false

# =================================================================
# LiveKit Configuration
# =================================================================
# リアルタイム音声/ビデオ通話機能
# 取得方法: https://cloud.livekit.io/ でプロジェクトを作成
#
# 注意: Cloud Functionsでも同じ値を設定する必要があります
#       firebase functions:config:set livekit.api_key="YOUR_KEY"

LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
NEXT_PUBLIC_LIVEKIT_URL=

# =================================================================
# 環境設定の注意事項
# =================================================================
# 1. このファイル（.env.example）はGitにコミットされます
# 2. 実際の値は .env.local に記載してください（Gitに含まれません）
# 3. 本番環境（Vercelなど）では Environment Variables に設定してください
# 4. 全ての必須項目を設定しないとアプリケーションは動作しません
